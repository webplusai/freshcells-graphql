# GraphQL

## Setup
1) Create `.env` (refer `.env.example`)

1) Install packages
    ```
    yarn install
    ```

1) Start development
    ```
    yarn dev
    ```

1) Run Test
    ```
    yarn test
    ```